import React from 'react';

const initialState = {
  start: []
}
function rootReducer(state = initialState, action) {
  /*  switch (action.type) {
     case 'SET': {
 
     }
     default:
     return state
   } */

  return state;
}

export default rootReducer;